public class myDATA {
    
//Map Concept to understand the basis.    
    
/*Map<string,string> map1 = new Map<string,string>();
List<Account> accList =[Select Id, Name From Account];
for (Account acc : accList){
      map1.put(acc.id,acc.name);
}
integer count = map1.size();   
system.debug('Total number of records : ' +count);
system.debug('Map on 1st index' +map1);
*/


}